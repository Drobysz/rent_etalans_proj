services:
  nginx:
    image: nginx:1.27-alpine
    depends_on:
      - app
      - admin_panel
      - api
    ports:
      - "80:80"
      - "8080:8080"
      - "8000:8000"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
    restart: unless-stopped

  app:
    build:
      context: ./app
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-/api}
        NEXT_PUBLIC_BASE_PATH: ${NEXT_PUBLIC_BASE_PATH:-/au_calme_campagne_etalans}
        NEXT_PUBLIC_BASE_URL: ${NEXT_PUBLIC_BASE_URL:-}
    env_file:
      - ./app/.env
    environment:
      NEXT_PUBLIC_BASE_PATH: ${NEXT_PUBLIC_BASE_PATH:-/au_calme_campagne_etalans}
      NEXT_PUBLIC_BASE_URL: ${NEXT_PUBLIC_BASE_URL:-}
    depends_on:
      - api
      - telegram_bot
    restart: unless-stopped

  admin_panel:
    build:
      context: ./admin_panel
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-/api}
    env_file:
      - ./admin_panel/.env
    depends_on:
      - api
    restart: unless-stopped

  api:
    build:
      context: ./api
    env_file:
      - ./api/.env
    depends_on:
      mysql:
        condition: service_healthy
    restart: unless-stopped

  telegram_bot:
    build:
      context: ./telegram_bot
    env_file:
      - ./telegram_bot/.env
    volumes:
      - telegram_bot_data:/data
    restart: unless-stopped

  mysql:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: ${DB_DATABASE:-rent_etalans}
      MYSQL_USER: ${DB_USERNAME:-rent_etalans}
      MYSQL_PASSWORD: ${DB_PASSWORD:-rent_etalans}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-root}
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped

volumes:
  mysql_data:
  telegram_bot_data:
